<h1> <%= @job.title %></h1>

<p><%= render_job_description(@job) %></p>

<div class="text-center">
  <%= link_to("send the resume", new_job_resume_path(@job), style: "font-size: 30px; text-decoration:underline;") %>

</div>
